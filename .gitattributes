# Ensure shell scripts always use LF so they run on the Linux VPS,
# regardless of the committer's OS (autocrlf on Windows).
*.sh text eol=lf
scripts/* text eol=lf
*.service text eol=lf
*.conf text eol=lf
